set(src_dirs
            LED)

set(include_dirs
            LED)

set(requires
            driver)

idf_component_register(SRCS "${src_dirs}"
                       INCLUDE_DIRS "${include_dirs}"
                       REQUIRES "${requires}")

component_compile_options(-ffast-math -03 -Wno-error=format=-Wno-format)